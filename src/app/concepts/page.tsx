import { ConceptsClient } from "./client";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Concepts · Mettrik",
};

export default function ConceptsPage() {
  return <ConceptsClient />;
}
