import { PriceTargetClient } from "./client";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Price target concept · Mettrik",
};

export default function PriceTargetConceptPage() {
  return <PriceTargetClient />;
}
