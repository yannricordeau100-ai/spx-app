# Reprise de conversation Mettrik, 21 septembre 2026

> Ecrit pour que Yann puisse changer de compte Claude et continuer sans rien
> perdre. Tout ce qui suit est verifie, pas suppose. Les points ouverts sont a
> la fin, avec ce qui a ete decide et ce qui attend un arbitrage.

## 1. Etat du deploiement

- Version locale et poussee : **2026.09.21.5**, commit `36a6f19246`.
- Preversion (niveau2) : a jour sur ce commit.
- Production (mettrik.ai) : servait `a4bea4de7b` au dernier go n0 reussi.
  **Le go n0 de la version 2026.09.21.5 n a pas encore ete lance.** C est la
  premiere chose a faire a la reprise : `bash scripts/go-n0.sh`.
- Rappel des pieges de la chaine : commiter les fichiers de veille AVANT le
  go n0 (`.batches-drafts-safe/kpis-haut/*.json`, `src/data/_*.json`,
  `src/data/v1-9-cron-history.json`), sinon le verificateur passe au rouge sur
  « modification non commitee ».

## 2. Ce qui a ete livre le 20 et le 21 septembre

### Graphiques moyen terme
- Valeurs ecrites a l horizontale sur les barres, sans le signe pourcent.
- Unite ecrite une seule fois au milieu de l axe vertical : `%` pour les
  pourcentages, sinon le champ `unite_axe` de la spec (exemple :
  « milliers d unites » sur le graphique Nvidia).
- Legende : espace franc entre les series.
- **Double affichage** : chaque graphique servi a un jumeau en PNG
  (`scripts/findings-png.js`, jumeaux generes uniquement pour les graphiques
  presents en base, liste passee par `--liste`). L apercu sert le PNG d abord,
  puis redescend sur le SVG, puis sur l image d origine. Cause : Safari
  refusait d afficher certains SVG locaux.
- Le nom du KPI d industrie couvert s affiche en gras sur la ligne de titre de
  la demande (colonnes `industry_kpi` et `industry_kpi_short` de
  `desk_image_findings`).
- La page GICS montre une pastille verte (graphique en ligne) ou ambre
  (graphique en attente) a cote de chaque KPI d industrie couvert.

### Fiches societe
- **Defaut majeur repare** : l injection des graphiques moyen terme, des
  indicateurs `v2-pipeline-specific-kpis` et de `v2-pipeline-exhaustive`
  vivait dans le bloc `if (enrich)` de `src/lib/company-core/load-company.ts`.
  Les societes sans fichier `v2-pipeline-enrich/<t>.json` perdaient tout.
  Les trois blocs sont sortis de cette condition.
- `KEPT_SOURCES` protege desormais aussi `v2-pipeline-specific-kpis` et
  `kpis-supplementary`. Gains mesures au chargement reel : CL 55 a 82,
  DIS 55 a 77, MU 61 a 82, BNP.PA 20 a 25, MC.PA 40 a 45, OR.PA 51 a 65,
  zero doublon, aucune societe saine modifiee.
- **Aucune source visible dans un bloc** : marche adressable, cartes de marche,
  repartition du chiffre d affaires, these et anti these, bloc comprendre la
  societe. Les 764 noms retires sont verses dans le mini bloc du bas
  (`src/data/sources-externes.json`), sans jamais dire a quoi ils servent.
  MarketBeat et StockAnalysis sont filtres a l affichage.
- Le mini bloc des sources est descendu tout en bas de la page.
- Bandeau « Cette societe a un ou plusieurs KPI exclusifs a Mettrik AI » :
  `src/components/bandeau-exclusif.tsx`, pose juste au dessus de la rangee des
  chips de rang, couleur de l accent de la societe. Liste en dur dans le
  composant : NFLX, AAPL, PLTR, MC.PA, RMS.PA, TTE.PA. Jamais de nombre.
- Navigation au clavier dans la recherche de societes : fleches, Entree, Echap.

### Support client, tout neuf
- Tables Supabase `support_tickets` et `support_messages`.
- `src/lib/support/tickets.ts`, `src/lib/email/support.ts`, routes
  `/api/support/tickets`, `/api/support/tickets/[id]`,
  `/api/desk-mtk9x4kp/support`.
- Bulle flottante : `src/components/support/*`, montee dans `layout.tsx`,
  chargee a part derriere une barriere d erreur. Recherche dans la FAQ locale
  ouverte a tous ; ecrire un message demande un compte.
- Interface proprietaire : `/sandbox/support`. Espace client :
  `/account/support`.
- Courriel vers `SUPPORT_EMAIL` (variable posee en production et en preversion,
  valeur `support@mettrik.ai`). Si l envoi echoue, le ticket est quand meme
  enregistre.

### Contenu et textes publics
- Aucune mention d indice boursier sur le site public : 111 mentions retirees,
  comptes verifies a zero sur l accueil, les tarifs et la FAQ, metadonnees et
  donnees structurees comprises, dans les quatre langues presentes.
- Netflix : le KPI « Ajouts nets d abonnes en diffusion en continu » va
  maintenant de 2015 a 2025 (23,4 millions pour 2025, deduit des deux sources
  publiees par la societe). Le mot « jalon » est banni, remplace par « source ».
- Captcha : integre au formulaire, en francais, case toujours cochable.

### Ateliers de recherche d indicateurs, construits mais NON lances
- `/sandbox/kpi-pistes` : cinq sous onglets (questions des analystes,
  regulateurs et federations, concurrents, referentiel par sous industrie,
  journees investisseurs). Table `desk_kpi_pistes`. Aucun lancement possible.
- `/sandbox/kpi-non-financiers` : saisie de tickers, six criteres cochables,
  propositions a cocher puis enregistrer. Table `desk_kpi_non_financiers`.
  **Une demande attend : MC.PA, creee le 21 septembre a 00h02, statut a
  traiter, zero proposition.** Elle n a pas ete lancee, conformement a la
  consigne de Yann.

## 3. Regles apprises ou durcies ces deux jours

1. **Aucun lien vers l outillage sur mettrik.ai.** Les prefixes `/sandbox`,
   `/admin`, `/desk-mtk9x4kp`, `/email-lab` repondent 404 sur le domaine
   public depuis le 19 septembre. Les outils et les pages `/concepts` se
   donnent sur `https://mettrik-niveau2.vercel.app`.
2. **Ne jamais approuver un graphique a la place de Yann**, sauf demande
   explicite et nominative.
3. **Aucune source visible dans un bloc de page societe.** Elles vont toutes
   dans le mini bloc du bas, sans attribution a un indicateur precis.
4. **Aucune liste d indices** sur le front public.
5. **Une seule conference de resultats** par societe pour chercher des idees
   d indicateurs, la derniere. Test sur MPWR, YUM et AMGN : la deuxieme
   conference apporte 57 pour cent d idees en plus pour 100 pour cent de cout
   en plus, et presque rien quand la fiche depasse 25 indicateurs.
6. **Economie de jetons sur les balayages** : isoler la section des questions
   par script avant toute lecture par un modele, jamais Opus pour ces passes.
7. Jamais le mot « jalon » dans un texte visible, ecrire « source ».

## 4. Points ouverts, par ordre d urgence

1. **Lancer le go n0** de la version 2026.09.21.5.
2. **Documents exportes de LVMH et Hermes** : la societe rattachee s affiche
   avec son code boursier brut (« RMS.PA ») sur une ligne separee au lieu de
   son nom ; et sur le graphique du PE previsionnel, la derniere colonne porte
   « 18 sept. 2026 » au milieu de colonnes annuelles. ETA annoncee a Yann :
   20 minutes en preversion, 40 minutes en production.
3. **Couche d indicateurs non etiquetee** encore ecartee par `KEPT_SOURCES`
   (base non etiquetee, `kpis-v3-verif`). A traiter avec le meme protocole de
   preuve avant et apres, en chargeant reellement des societes temoins.
4. **Defauts 2, 3, 6, 7, 9 et 10 du rapport `/tmp/audit-circulation.md`** :
   blocs de gouvernance coupes globalement (512 societes), coupe de masse du
   9 aout jamais revue (93 societes pour les evenements), filtres silencieux.
   Ce sont peut etre des choix de Yann, a lui soumettre avant de toucher.
5. **Infobulles « Preuve » des theses** : elles contiennent des citations mot
   pour mot ou la source apparait parfois dans la citation. Non touchees,
   arbitrage attendu de Yann.
6. **Demande MC.PA** de la page des indicateurs non financiers, en attente de
   son feu vert.
7. **Avertissement orange du go n0** : des dossiers lus par l application sont
   exclus du paquet Vercel (`src/data/companies`, `src/data/v1-9-complete`,
   `src/data/v2-pipeline-exhaustive`, `src/data/v2-pipeline-i18n`). Lectures
   de repli protegees, donc sans panne, mais `v2-pipeline-i18n` ne devrait
   plus etre lu du tout depuis l abandon des traductions.

## 5. Ce que Netflix a recu

- Graphiques moyen terme approuves : taux de resiliation mensuel aux Etats
  Unis (dix releves, 2023 a 2026) et part des abonnes britanniques ayant
  resilie en trois mois (2022 a 2025, quatre annees continues). Tous deux
  rattaches au KPI d industrie `CHURN_STREAMING`, qui figure bien au
  referentiel de la sous industrie 50202010 comme indicateur distinctif.
- Graphiques en attente : heures visionnees par semestre (six semestres), part
  de l audience televisee americaine, foyers britanniques equipes.
- Page de propositions : `/concepts/kpi-netflix`, 23 indicateurs non
  financiers avec source, citation, valeurs et note de fiabilite.

## 6. Ce qui n existe pas, verifie et acte

- **Heures visionnees Netflix a une maille plus fine que le semestre** : rien,
  ni chez Netflix ni chez aucun mesureur tiers. Nielsen, Barb, Ofcom, Digital i,
  Ampere, Omdia ne publient que des parts d audience ou des minutes par titre.
- **Ajouts nets d abonnes Netflix pour 2025** : aucun chiffre publie, seulement
  deux seuils annonces. La valeur de la fiche en est deduite et le dit.
- **Taux de resiliation Netflix trimestriel sur cinq ans** : n existe pas. Les
  rares chiffres trimestriels reposent sur une definition ancienne, non
  comparable.
