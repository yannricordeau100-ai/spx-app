import { FicheSquelette } from "@/components/fiche-squelette";

/** Retour instantane pendant l assemblage serveur de la fiche (8 sept 2026). */
export default function Loading() {
  return <FicheSquelette />;
}
