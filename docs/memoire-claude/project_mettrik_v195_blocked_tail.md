---
name: project_mettrik_v195_blocked_tail
description: "Mettrik V195 - les tickers non-online restants sont bloques pour des raisons structurelles, ne pas re-tenter a chaque run"
metadata: 
  node_type: memory
  type: project
---

MAJ 28 aout 2026 (run planifie) : queue **fermee**, 862/871 en ligne, les 9 restants requalifies = 0 PASS. ALAB, AMRZ.SW, NBIS, SNDK, SPCX, HONA, Q : IPO/spin-off recents, 2 a 14 trimestres reels, profondeur 16 inatteignable. FDXF = doublon OTC de FDX deja en ligne (pas de fiche companies/fdxf.json). **FLEX tranche definitivement** : le 10-K FY2026 (20 mai 2026) remplace les segments FAS/FRS par ITS, RMS et CPI sans retraitement trimestriel, donc le plus gros segment (ITS, 3 056 M$ au Q1 FY27) n'a que 2 trimestres ; la serie FAS de 19 trimestres verifiee existe (/tmp/flex-sec/fas-series-verified.json) mais le segment est supprime, hero mort, ne pas publier. Aucune source SEC locale pour FLEX, Q ni FDXF dans ~/Mettrik/sec-data. Le pipeline CA n'a plus aucun bloc a traiter.

MAJ 21 aout 2026 01:55 (run planifie) : **fin de la serie de 24 runs satures, du travail reel est apparu**. L'univers `src/data/v1-9-5-clean-all-tickers.json` est passe de 651 a 656 (commits `289ab663c3` et `25785185da`, integration SP500 2026). 4 steps publiees, online-tickers 873 -> 877 :
- **FERG** (Ferguson) : qualifieur PASS direct, hero = BRANCHES (1 746 succursales, 5 exercices).
- **VMRK** (Vivmark Residential, fusion AvalonBay + Equity Residential du 17 aout 2026) : le rendu tombait sur `AVB_revenue_total` (CA total ex-AvalonBay) car le prefixe `AVB_` echappe a `isTotalRevenueLabel` (meme famille de defaut que [[project_mettrik_hero_selection_defect]]). Repointe sur `AVB_apartment_homes_owned` (98 271 logements, 49 trimestres). **Piege majeur** : ni `apply-hero-fix.py` ni l'edition de `.batches-drafts-safe/kpis-haut/<T>.json` ne suffisent, `loadV17Company` recalcule le hero. Seule solution qui gagne : upsert dans la table Supabase `desk_hero_kpi_overrides` (ticker, hero_kpi_short), appliquee tout a la fin de load-company.
- **BNY** et **ECHO** : renommages boursiers 2026 de BK et SATS, renommage profond deja fait dans les data (bk.json et sats.json n'existent plus). Qualifieur PASS, il suffisait de les publier.

Nouveaux bloques structurels ajoutes a `src/data/_hero-suspect.json` (ne pas re-tenter) :
- **FLEX** : refonte de segments au T1 FY2027 (juin 2026), ITS/RMS/CPI remplacent Flex Agility Solutions et Flex Reliability Solutions. La serie FAS extraite verbatim (20 trimestres, `/tmp/fix-flex.json`) s'arrete au 31 decembre 2025 (segment supprime) et la nouvelle segmentation n'a que 2 trimestres retraites. **Non applique volontairement** : appliquer poserait `_validation=true` sur un hero perime et le cron le publierait. Arbitrage Yann.
- **FDXF** : spin-off FedEx Freight du 1er juin 2026, 8 trimestres calendaires au maximum, 3 exercices annuels dont 2 avec statistiques d'exploitation, aucun 10-Q.

Pipeline CA relance (PID 59864) : **150 blocs a traiter** (les 5 nouvelles steps), fini en 12 min, ok=2 reject=141 no_source=7 llm_fail=0. Les rejets sont legitimes (`slices<2` sur des utilities et banques US mono-geographie). Deploye et verifie : `tsc --noEmit` = 0 erreur, alias pose deux fois, api = 200, 877 tickers dont FERG/VMRK/BNY/ECHO.

Restent 12 bloques inchanges (ALAB AMRZ.SW BG GFS HONA NBIS PSKY Q SNDK SOLV SPCX TKO) + les 2 nouveaux ci-dessus, soit 14 non-online sur 656. Detail des 12 : spin-offs ou IPO de moins de 5 ans, sec-data locale sans les exhibits trimestriels EX-99.1, ou segment supprime par une refonte (BG). Toujours en attente d'arbitrage Yann sur `isGenericKpi` (underscores).

**Regle de reprise** : ne plus se contenter de comparer online-tickers a un chiffre memorise. Recharger `src/data/v1-9-5-clean-all-tickers.json` a chaque run et diffuser la difference : l'univers bouge quand Yann integre des entrants d'indice. Un `git log` sur ce fichier dit immediatement ce qui a change. Voir [[project_mettrik]] et [[reference_mettrik_apply_hero_fix]].

MAJ 21 aout 2026 05h30 (run planifie apres reset de fenetre) : **aucun backlog de publication restant**. 656 dans clean-all, 642 online, les 14 non-online sont les bloques structurels deja listes ci-dessous. Les 187 autres tickers de `src/data/v1-9-universe.json` (FTSE100, TSX60, FTSE MIB, ATX, BEL20) sont hors clean-all, donc invisibles dans la search par construction (`company-search.tsx` filtre sur V195_CLEAN_ALL_SET) : les publier ne sert a rien tant que Yann ne les integre pas a clean-all. Ne pas relancer le qualifieur dessus, il repond "hors clean-all (pas de fiche)".

Deux incidents traites ce run :
- L'alias `mettrik-niveau2.vercel.app` pointait sur un deploiement **Production**, dont l'appel Supabase echoue : la search N2 renvoyait 0 sté. Repointe sur le preview staging. **Verifier l'alias au debut de chaque run**, un deploiement prod de Yann peut le capturer.
- La **production** `mettrik.ai` est cassee, ses variables Supabase pointent sur l'ancien projet `cnggtyxzqlqqjrynnvdq` (injoignable) alors que Preview utilise `idpsbtgvuyfwtvzelogw`. Non corrige (hors mandat N2), documente dans `.conv-state/INCIDENT-prod-supabase-2026-08-21.md` et notifie a Yann.

Audit qualifieur des 642 visibles : 636 PASS, 6 FAIL. SIKA.SW corrigee (override Supabase pointait sur le CA total, cf. [[reference-mettrik-hero-override-supabase]]). APP bloquee (mono-segment depuis la cession du segment Apps au T2 2025, Advertising = CA total). RDDT bloquee jusqu'au T4 2026 (DAUq publie sur 14 trimestres seulement, les 6 trimestres 2021-2022 n'existent que dans un graphique du S-1). CRWV, GEV et SW bloquees par profondeur post IPO ou scission. Detail dans `.conv-state/v195-audit-2026-08-21.json`.

MAJ 21 aout 2026 ~06h15 (run planifie apres reset) : **run sature, aucun backlog**. Univers inchange (656 dans clean-all, dernier commit sur le fichier = `25785185da`), 877 online cote API dont 642 dans clean-all, les 14 non-online sont toujours les bloques structurels (ALAB AMRZ.SW BG GFS HONA NBIS PSKY Q SNDK SOLV SPCX TKO FLEX FDXF). Pipeline CA : `0 (ticker,block) a traiter`, rien a relancer. Alias verifie sain avant et apres (api = 200, 877 tickers, pas d'erreur Supabase). Qualifieur repasse sur les 5 stes touchees au run precedent : FERG VMRK BNY ECHO SIKA.SW = 5/5 PASS.

Seul travail reel : deux corrections yoy laissees non commitees par un run anterieur (MS CET1 14,8 -> 14,9% et yoy -0,1 pts au T2 2026 ; PH aftermarket aeronautique +27,3% -> +22,0%, verifie 3132/2568,2). Commit `0811e2f598`, deploye et alias pose.

**Pieges CLI notes ce run** (utiles aux prochains) : `timeout` n'existe pas sur ce Mac ; `npx vercel ls 2>/dev/null` n'affiche que les URL, la colonne Status part sur stderr, donc **toujours `2>&1`** pour lire Building/Ready ; un build staging prend 9 a 11 min.

MAJ 21 aout 2026 ~15h10 puis ~20h05 (runs planifies apres reset) : **runs satures, aucun backlog, zero publication**. Univers `v1-9-5-clean-all-tickers.json` inchange (656, dernier commit toujours `25785185da` du 19 aout), 877 online cote API dont 642 dans clean-all, non-online = les 14 bloques structurels connus (ALAB AMRZ.SW BG FDXF FLEX GFS HONA NBIS PSKY Q SNDK SOLV SPCX TKO). Alias `mettrik-niveau2.vercel.app` sain (api 200, pas de capture par un deploiement prod). Pipeline CA relance a chaque run : `0 (ticker,block) a traiter`, fini en 2 s. Aucun commit data depuis `0811e2f598`, donc l'audit exhaustif des 642 du run de 05h30 fait toujours foi et il n'y a rien a re-qualifier.

Deux constats a garder :
- `/v1-9-5/<T>` en anonyme renvoie **307 vers `/?auth=signin&next=...`** : c'est le gate d'authentification qui fonctionne, pas une fiche cassee. Ne pas confondre avec le redirect index silencieux du gate de visibilite ([[reference-mettrik-visibility-gate]]), qui lui arrive **apres** login. Toute verification de rendu doit passer par le compte test ([[feedback-mettrik-verify-admin]]).
- Le working tree porte des modifications **dormantes depuis juillet** sur `CLAUDE.md`, `HANDOFF.md` et `RULES-GOLDEN.md` (dates mtime 4 et 8 juillet) : elles **regressent** l'etat de HEAD (suppression des 214 lignes de RULES-GOLDEN.md, retour de HANDOFF.md a la version du 8 juillet, retrait de `@SHARED-STATUS.md` dans CLAUDE.md). Ne jamais les inclure dans un `git add` de lot data. Arbitrage Yann pour trancher entre `git checkout` et commit.

MAJ 22 aout 2026 ~00h30 (run planifie apres reset) : **aucun backlog de publication, mais du vrai travail data trouve et deploye** (commit `39f2543ece`). Univers inchange (656 clean-all, dernier commit `25785185da`), 877 online dont 642 dans clean-all, memes 14 bloques structurels. Alias verifie sain avant et apres, api 200.

Trois enseignements reutilisables :
- **La file `.conv-state/eu-earnings-refresh-todo.json` est un faux positif quasi total.** Les noms de fichiers du data-lake portent la **date de telechargement**, pas la date de publication : `ENGI.PA_RFS_S12025_2026-08-02.pdf` est le rapport du 30 juin **2025**. 23 des 25 tickers marques `a_rafraichir` avaient deja leur document dans `documents_integres` de `.conv-state/eu-earnings-refresh-state.json`, et les 2 restants (CA.PA, ENGI.PA) ont ete verifies verbatim comme deja a jour. **Pre-filtrer par le state file avant de lancer le moindre agent** : 3 agents a ~80k tokens pour 1 seul defaut reel.
- **Le seul test programmatique fiable de fraicheur** : pour chaque KPI `short` present a la fois dans `.batches-drafts-safe/kpis-haut/<T>.json` et dans `src/data/v2-pipeline/<t>.json`, comparer la periode du dernier point d'`history`. 7 stes en derive reelle trouvees et corrigees (ABN.AS CLIENT_ASSETS_Q, BNR.DE RBO, CRWV rpo_q, ENTG REVENUE, FER REV_CONSTRUCTION_Q, G1A.DE CDE, HNR1.DE RN_Q, toutes Q1-2026 -> Q2-2026). Comparer `last_data_date` ne marche pas (kpis-haut y met la date de publication du document, la fiche la fin de periode), et comparer le hero aux `short` de la fiche ne marche pas non plus (360 faux positifs sur 642 : le loader resout le hero via d'autres sources).
- **SCMN.SW** : la fiche etait restee a Q1-2026 avec son hero `MOBILE_CH` totalement absent de la fiche (heroKpiUsable casse). Corrige, serie hero de 22 points reinjectee. Cette derive fiche/kpis-haut est une famille de defaut distincte du hero override Supabase ([[reference-mettrik-hero-override-supabase]]).

Egalement commitees : trois corrections d'identite laissees non commitees par un run du 21 aout 14h21 (EN.PA decrit comme un organisme de transport fluvial au lieu de Bouygues, FGR.PA, KNIN.SW), les 3 PASS au qualifieur. Le working tree porte toujours ~8500 fichiers modifies par d'autres sessions (surtout `src/data/att/*`, ecrits jusqu'a 23h59) : **ne jamais faire de `git add src/data`**, ajouter les fichiers un par un et verifier le perimetre reel de `_merged.json` en le diffant contre `git show HEAD:` cle par cle.

Pieges CLI de ce run : ce shell est **zsh**, `set -- $var` ne fait pas de decoupage en mots (utiliser une fonction avec `$1 $2`) ; `build-v2-pipeline-merged.ts` imprime la liste complete des 2307 tickers, la piper dans `grep -v Tickers`.

MAJ 22 aout 2026 ~05h10 (run planifie apres reset) : **aucun backlog de publication, mais une famille de defaut d'affichage trouvee et corrigee sur 16 fiches**. Univers inchange (656 dans clean-all, dernier commit toujours `25785185da`), 877 online cote API dont 642 dans clean-all, memes 14 bloques structurels. Alias sain avant intervention, pipeline CA relance puis `0 (ticker,block) a traiter`. Le test de fraicheur kpis-haut contre v2-pipeline (methode du run de 00h30) renvoie **0 derive** : le resync precedent tient.

Audit qualifieur des 642 : **637 PASS, 5 FAIL** et les 5 sont les blocages connus (APP mono-segment, CRWV 7 trim, GEV 10 trim, RDDT 14 trim, SW 10 trim). Aucune regression.

**Nouvelle famille de defaut : l'unite des blocs de repartition du CA.** `repartition-block.tsx` rend `revenue_by_segment` et `revenue_by_geography` en deux onglets et prend l'unite au **niveau du bloc**, pas des parts ; quand elle manque il applique un repli `Mds $`. Quinze fiches affichaient donc un facteur 1000 (ZBH montrait 4 764 Mds $ pour un CA de 8,2 Mds $, NVR 4 372 Mds $, MSCI 922 Mds $, HBAN, KEY) ou rien du tout (ABBV, PRU, OXY, BX, STX, EXC, CBK.DE, FER, MTX.DE, QIA.DE), et NUE affichait ses parts en pourcentage etiquetees `Mds $` (61 Mds $ pour steel mills). Corrige en posant la seule unite, **aucune valeur touchee**, facteur ancre sur le CA total de la fiche ou sur l'autre bloc. Commits `7f43fb30dd`, `0e7e73b9c4`, `11b5aef331`.

**Methode de detection reutilisable** (peu couteuse, a relancer aux prochains runs) : pour chaque ste online et pour les deux blocs, comparer le total des parts au CA total de la fiche et a l'autre bloc. Un total >= 500 avec une unite en `Mds` ou sans unite est un defaut certain ; un total entre 98 et 102 avec une unite monetaire signale des pourcentages mal etiquetes. Le test brut sur l'ecart des totaux entre segment et geographie donne 253 resultats et est **inexploitable tel quel** : les deux blocs couvrent legitimement des perimetres ou des exercices differents.

**Cas ecartes apres verification, ne pas les re-signaler** : BAC (100,0 sont de vrais Mds $), SNA (parts a 117% car segments avant eliminations, le composant recalcule les proportions sur les valeurs donc l'affichage est juste), ARES et KKR (les blocs geographiques portent des AUM et le label le dit), AMZN WMT TSM (totaux eleves mais exacts).

**Restent en arbitrage Yann**, releves dans `.conv-state/repartition-doublons-2026-08-22.json` et volontairement non modifies : quatre fiches ou les deux blocs couvrent la meme dimension avec un des deux perime (ZBH, HSY, EPAM garder la geographie ; FDS garder le segment), et trois fiches online sans aucun onglet de repartition (EXO.AS, SIE.DE, VMRK) alors que la file du pipeline CA est vide, ce qui pointe un reglage du pipeline plutot qu'une extraction a faire a la main.

Piege CLI de ce run : un `git push origin staging` declenche **deja** un deploiement automatique, donc appeler `VERCEL_DEPLOY_HOOK_STAGING` en plus empile des builds inutiles (5 en parallele ce run). Pousser d'abord, ne declencher le hook que si aucun build ne demarre.

MAJ 22 aout 2026 ~10h-11h (run planifie apres reset) : **aucun backlog de publication** (656 dans clean-all, dernier commit toujours `25785185da`, 877 online dont 642 dans clean-all, memes 14 bloques structurels), mais **trois familles de defaut d affichage trouvees et corrigees sur 22 fiches**, en trois commits deployes et alias pose (`aaf904aac4`, `ee4fedf254`, `14286b9d86`, deploiement `mettrik-psjtue90d`).

**Le garde-fou `block_valid_in_data` du pipeline CA ne testait que le nombre de parts.** Un bloc `revenue_by_*` avec 2 parts ou plus mais aucune valeur ni pourcentage exploitable passait pour valide et n etait jamais re-extrait : la fiche affichait une liste de libelles sans le moindre chiffre. Corrige (2 parts chiffrees minimum), plus une table `SOURCE_ALIAS` pour les renommages et fusions 2026 (VMRK->AVB, BNY->BK, ECHO->SATS : sans elle VMRK sortait `no_source` a chaque run car son 10-K est depose sous AVB), plus le log de **chaque** rejet et non un sur dix.

Les trois familles, dans l ordre ou les tests les trouvent :
1. **Bloc sans aucun chiffre** (9 blocs) : CBRE MO STT segments, EG ICE MCHP geographies extraits verbatim des 10-K ; JKHY ramene au format mono-geographie standard ; L et PFG bloc geographique retire (le 10-K ne ventile pas le CA par zone).
2. **Une part sans valeur mais avec un pourcentage** (8 fiches) : le treemap dimensionne sur `value`, la part disparaissait et les autres etaient surdimensionnees. INTC ORCL SWK ON MSCI CL CEG MET. Plusieurs portaient en prime une segmentation perimee (MET avant la reorganisation 2025, MSCI des montants de 2022, SWK un segment cede).
3. **Les deux onglets ne couvrent pas la meme periode** : GOOGL avait un bloc par activite **trimestriel** face a un bloc geographique **annuel**. CCI (Fibre cedee), MCK et WFC traites aussi.

**Tests reutilisables a relancer a chaque run** (tous peu couteux, ecrits dans l historique de ce run) :
- parts sans valeur : pour chaque bloc, compter les slices avec `value > 0` ; moins de 2 = defaut certain.
- valeur contre pourcentage : `value / total * 100` compare a `share_pct`, ecart > 8 points = defaut.
- echelle : total du bloc < 0,5 avec une unite en `Mds` = facteur 1000 (c est ainsi que le bloc VMRK produit par le LLM a ete pris : il avait divise des milliers de dollars par un milliard).
- coherence des deux onglets : rapport des totaux >= 1,4, **bruite**, releve dans `.conv-state/repartition-ecarts-2026-08-22.json` (56 cas, une partie legitime : AUM chez ARES et KKR, perimetres differents). **Gisement restant, a traiter par lots aux prochains runs.**

**Piege majeur du qualifieur** : `xargs npx tsx scripts/qualify-stes.ts` **sans avoir source `.env.local` dans le shell courant** produit de faux FAIL. Sans `SUPABASE_SERVICE_ROLE_KEY` les overrides de hero ne sont pas charges et le hero calcule n est pas celui qui est rendu : MCHP PFG INTC sortaient FAIL puis PASS une fois l env charge. Voir [[reference-mettrik-hero-override-supabase]]. Les 22 fiches touchees ce run sont 22/22 PASS.

Autres notes : mon premier script de detection lisait `parts`/`items`, **la vraie cle est `slices`** (il renvoyait 0 defaut partout, faux negatif total). Les 89 fiches avec un bloc geographique a une seule part `Etats-Unis 100 %` unite `%` sont un **standard maison volontaire** (`single_region_legitimate`), pas un defaut. Le pipeline CA rejette legitimement les geographies de REIT et de banques US mono-pays. **Chantier editorial identifie, non entrepris** : 451 fiches / 1233 parts portent un libelle anglais identique a `label_en` (le dictionnaire `FR_LABELS` de `repartition-block.tsx` est tres partiel) : arbitrage Yann avant de lancer une traduction de cette ampleur.

MAJ 22 aout 2026 ~15h-16h (run planifie apres reset) : **aucun backlog de publication** (656 dans clean-all, dernier commit toujours `25785185da`, 877 online dont 642 dans clean-all, memes 14 bloques structurels ALAB AMRZ.SW BG FDXF FLEX GFS HONA NBIS PSKY Q SNDK SOLV SPCX TKO), mais **trois lots de defauts trouves et deployes** (`04c1ceb3c9`, `f4ed29cc31`, `1cf1295226`, `99687e66d9`, deploiement `mettrik-6e3evlqrd`, alias pose, api 200 / 877 tickers, qualifieur 21/21 PASS sur les fiches touchees).

**L alias avait de nouveau ete capture par un deploiement Production** (celui qui sert aussi mettrik.ai) : la search N2 renvoyait `TypeError: fetch failed`. Repointe sur le preview staging. C est la deuxieme fois : ce test doit rester le tout premier geste de chaque run. La prod mettrik.ai reste cassee sur le meme sujet (variables Supabase sur l ancien projet `cnggtyxzqlqqjrynnvdq`, NXDOMAIN) mais le site public est en page maintenance, donc **aucun impact visiteur** ; incident deja documente et notifie, hors mandat N2.

**Nouvelle famille de defaut : des octets de controle avaient remplace chaque caractere non ASCII** dans 109 champs visibles de 16 fiches. Libelles de blocs ("Rpartition du chiffre d affaires"), titres et resumes de risques ("cots salariaux", "Cybersurit", "Dpendance"), description de societe, unite "Mds " au lieu de "Mds EUR". Pieges et methode, reutilisables :
- Les controles sont **echappes en   dans le fichier** : lire le texte brut ne trouve rien, il faut `json.load` puis parcourir les chaines. Test : la classe de caracteres `\x00-\x08`, `\x0b`, `\x0c`, `\x0e-\x1f`, `\x7f`.
- Le code varie par fiche (00, 02, 1a, 1f) et **l information sur la lettre est perdue** : aucune reconstruction par dictionnaire ne marche (le corpus contient beaucoup de textes FR volontairement sans accents, donc la variante sans accent gagne le vote et on introduit des fautes).
- **AMP.MI et PTC font exception** : le codepoint hexa a survecu apres le controle (formes `0000e9`, `e9`, `e2019`, ou juste le dernier digit : 9 = e accent aigu, 8 = e accent grave, 0 = a accent grave, 5 = euro). Decodables mecaniquement, mais **ne jamais appliquer la regle hexa a 2 caracteres aux autres fiches** : dans "d?fense" le "fe" se lit 0xFE et donne "dthorn-nse".
- Pour les 12 autres fiches, textes restitues a la main puis **valides programmatiquement** : le squelette ASCII (tous les caracteres < 128) doit rester identique au caractere pres, ce qui garantit qu aucun chiffre ni mot anglais n a bouge. 74 corrections, 0 rejet apres 3 exceptions assumees et listees dans le script.
- Verification finale : valeurs numeriques et cles comparees a `git show HEAD:` fiche par fiche, identiques.

**Piege majeur d ecriture des fiches : l indentation JSON varie d un fichier a l autre** (1 espace pour cpay fslr jpm phm ptc rop, 2 pour les autres). Ecrire avec un `indent` fixe produit un diff de **9 394 lignes pour 74 champs**. Toujours detecter l indentation d origine sur la deuxieme ligne de `git show HEAD:<fichier>` avant de reecrire : le diff retombe a 110 lignes. Et `git add src/data/v2-pipeline/` ramasse les `.bak` laisses par d autres sessions, ajouter fichier par fichier.

**Blocs de repartition incoherents avec le CA de la fiche** (suite du gisement du run precedent, mais avec un test plus tranchant que le rapport entre les deux onglets) : comparer chaque total au **CA total de la fiche**. Cinq blocs sortaient a plusieurs fois le CA reel. **Methode de re-extraction qui marche** : retirer le bloc de la fiche, retirer le couple (TICKER, block) de `/tmp/ca-pipeline-checkpoint.json`, relancer `ca-pipeline-autonome.py` : il ne voit que ces blocs et les re-extrait. GE (geographie a 261,9 Mds $ pour un CA de 45,9), UBER (11,1 pour 52,0) et BKNG (6,8 pour 26,9) sont revenus avec un total **exactement egal** au CA. AVB (geographie a 8,0 pour un CA de 3,0) et DD (segments DowDuPont d avant 2020 a 20,4 quand le CA post-scission Qnity est de 6,849) ont ete **retires**, le pipeline rejetant la re-extraction (`slices<2`) : mieux qu un onglet faux, meme arbitrage que L et PFG.

**Ne pas relire `.conv-state/repartition-ecarts-2026-08-22.json` tel quel** : il date d avant les corrections du run precedent et fait ressortir GOOGL, deja traite. Regenerer le test a chaque fois.

**Nouveau gisement, releve dans `.conv-state/unites-kpi-mds-2026-08-22.json` : les KPI en millions etiquetes en milliards.** Test : unite commencant par `Mds` et valeur >= 400. 392 cas sur 111 stes, dont 8 servaient de hero. Trois corriges apres verification une par une, unite seule modifiee : ORLY (ventes aux particuliers et professionnels a 8 765 "Mds $" pour un CA de 17,4), CIEN (Products a 3 822 pour un CA de 4,8), HRL. **Correction de masse volontairement non faite** : sur 13 cas limites sondes, **6 etaient legitimes** (capex de marche annonces chez CIEN et SPGI, volume reseau et depenses cartes Platinum chez AXP, tresorerie clients SCHW, obligations de prestation restantes ORCL et MSFT a 638 et 627 Mds $ reels, actifs investis UBS a 7 326). Le libelle ne tranche pas : un segment s appelle "Automotive" ou "Lithium", un encours s appelle "Customer Equity". Verifier ste par ste contre le filing.

Autres tests relances, **zero regression** : parts sans libelle 0, echelle suspecte 0, value contre share_pct 2 faux positifs seulement (BMW.DE et V, dont les parts incluent une ligne negative d eliminations, cas deja ecarte pour SNA).

**A signaler a Yann** : une autre session ecrivait sur `.batches-drafts-safe/kpis-haut/CBRE.json` pendant ce run (mtime a 2 minutes) et y posait des yoy qui **ne collent pas a l historique du fichier** : Core EBITDA +33,5 % alors que 658 vers 836 fait +27,1 %, et CA total +15,5 % alors que 9 754 vers 11 226 fait +15,1 %. Non touche pour ne pas telescoper cette session.

MAJ 22 aout 2026 ~20h-21h (run planifie apres reset) : **aucun backlog de publication** (656 dans clean-all, dernier commit toujours `25785185da`, 877 online dont 642 dans clean-all, memes 14 bloques structurels), mais **51 fiches corrigees en cinq commits deployes** (`0a8cab8704`, `66ffe58b34`, `dd93535e6d`, `4d19d0bdf0`, `1f47a1acae`, `064fc6b0a8`, deploiement `mettrik-eaxdel116`, alias pose, api 200 / 877 tickers).

**L alias avait de nouveau ete capture par un deploiement Production** (api = `TypeError: fetch failed`). Troisieme fois. Repointe sur le preview staging. Ce test reste le premier geste de chaque run.

**Le test de fraicheur fiche contre kpis-haut de la methode du 00h30 etait un faux negatif total.** Cote `.batches-drafts-safe/kpis-haut/<T>.json` la periode d un point d history est la cle **`q`** (`{"q":"Q2-2026","v":263187}`), pas `period` ni `date` ; cote `src/data/v2-pipeline/<t>.json` l history est une **liste de nombres bruts sans periode**, donc les periodes ne sont pas comparables du tout. Le test qui marche : comparer `value` de la fiche a `history[-1]` de kpis-haut, et ne retenir que les cas ou la fiche porte exactement `history[-2]`. 541 comparaisons, 18 candidats, **17 dérives reelles** (la fiche etait restee au T1 2026 quand kpis-haut avait deja le T2). Confirmation par `last_data_date` : la fiche datait de mars ou mai, kpis-haut du 30 juin. Corriges : AJG AME EFX ETN F GS INTC MAR (2 KPI) ODFL PG PNC PYPL RCL RKLB STLAP.PA VLO. **PODD ecarte, sa fiche est plus fraiche que kpis-haut** : le test doit se lire dans les deux sens.

**Le gisement des ecarts entre les deux onglets de repartition est traite**, il tombe de 46 a 13 cas sur les fiches visibles. Le test tranchant n est pas le rapport entre les deux totaux mais **la comparaison de chaque total au CA reel de la sté** : sur ces fiches un onglet collait au CA et l autre couvrait un trimestre, un perimetre partiel ou une segmentation perimee (DIS avant la reorganisation 2023, AMD avant la montee du Data Center, AIG avec Life and Retirement cede avec Corebridge, ALB avant la baisse du lithium). Methode : retirer le bloc faux, retirer le couple du checkpoint, relancer `ca-pipeline-autonome.py`. **14 re-extractions revenues exactement au niveau de l autre onglet** (ACGL ADI ALB APTV CDNS EG GPC LDOS MRSH ROP VRTX WY XYL XYZ), **16 blocs retires** apres rejet `slices<2` ou re-extraction pire que l original (AIG AMD AME AZO BA CAT CF COHR DIS EME FSLR IT LULU MGM NN.AS RTX SJM TDY VRT), meme arbitrage que L et PFG. Les 13 ecarts restants sont legitimes ou en arbitrage et **ne doivent plus etre re-signales** : AUM chez ARES et KKR, revenus nets contre bruts chez C JPM MS RJF MCO IBKR, ligne `Other (oil purchases)` chez APA et EOG, HLT hors remboursements de couts, APP et EPAM en arbitrage.

**Piege majeur, nouveau : un bloc retire de la main d une session ne tient pas.** Le pipeline CA a **re-extrait les blocs segment de GE et DELL** que le commit `ac75920cb4` avait volontairement retires quelques heures plus tot, simplement parce que le couple n etait pas inscrit dans `/tmp/ca-pipeline-checkpoint.json`. Les nouvelles extractions se sont trouvees justes (GE segment 45,86 contre geographie 45,85 ; DELL 111,81 contre 113,54) et ont ete conservees, mais **tout retrait volontaire doit etre accompagne d un ajout du couple `[TICKER, block]` dans `done` du checkpoint**, sinon il sera annule au prochain run. Le checkpoint vit dans `/tmp`, donc ce verrou est fragile : a signaler a Yann.

**KPI de revenu en millions etiquetes en milliards, 51 corriges sur 17 fiches** (unite seule modifiee). Le releve brut du run precedent (392 cas, 6 faux positifs sur 13 sondes) etait inexploitable ; le test cible qui marche croise trois conditions : unite commencant par `Mds`, **nom du KPI contenant un terme de revenu** (ce qui ecarte AUM, TPV, encours, obligations de prestation restantes, qui depassent legitimement le CA), et valeur superieure a **dix fois le total du bloc de repartition de la meme fiche**. CMG restauration 11 866 pour un CA de 11,93, LEN 32 097 pour 34,39, UHS 12 278 pour 17,35. Validation croisee gratuite : sur chaque fiche les autres KPI portaient deja `M $` au meme ordre de grandeur. **SO ecarte** : son KPI `revenue` a 29,553 Mds $ est bien le CA total, c est son bloc de repartition a 1,75 qui est faux (reste a traiter).

**Les 45 blocs de repartition sans aucune valeur chiffree sont tous hors clean-all**, donc sur des pages en redirect (`src/app/sandbox/v1-9-5/[ticker]/page.tsx` ligne 248). Ne pas les corriger : UL AZN.ST GLEN.L MUFG NMR NSRGY RELX BJ ILMN JAZZ MEDP UNM VIAV WMS NLY PSTG HLI et les autres. Le treemap dimensionne **exclusivement sur `d.value`** (`repartition-variants.tsx`), et `hasGeo`/`hasSegment` ne testent que `slices.length > 0` : un bloc dont les parts n ont que `share_pct` s affiche donc vide. Si Yann integre un jour ces tickers a clean-all, la reparation mecanique est de poser `value = share_pct` et `unit = "%"` sur les 31 blocs dont les parts somment a 100.

Pieges CLI de ce run : un `until` de polling sur `npx vercel ls` depasse le timeout de 10 minutes de l outil, poller par appels separes ; cinq pushes successifs empilent cinq builds de 8 minutes, mieux vaut grouper les lots.


MAJ 27 aout 2026 ~10h-11h (run planifie apres reset) : **l univers a bondi de 656 a 862 dans la nuit** (Yann a integre 206 tickers, commits `66871dcc8c`, `146bce502d`, `68eee79c83`, `317c5d07dd`). **9 stes publiees ce run**, 3 commits deployes (`4902a1c69a`, `cb52de6af2`, `72f449e744`), clean-all a 871.

**Audit qualifieur des 848 en ligne : 842 PASS, 6 FAIL, aucune regression** malgre les 206 entrants. Les 5 FAIL structurels connus tiennent (APP, RDDT, CRWV, GEV, SW) plus GOOGL, corrige.

Publiees : **SAND.ST, NDA-FI.HE, EDP.LS, MAP.MC, UNM, NBIX, DGE.L, CLNX.MC, SPM.MI**. Aucune n avait d equivalent deja en ligne.

**Le gisement le plus rentable du moment : les fiches bloquees au seul seuil des 4 KPI specifiques.** Leur hero est deja valide, il suffit d extraire 3 KPI de plus par agent depuis `cat3-european/<T>/annual-text`. Quatre stes debloquees en une vague de 3 agents. `apply-hero-fix.py` accepte un champ **`extra`** (liste de KPI upsertes dans base.kpis) : c est l outil pour ajouter des KPI sans toucher au hero, il suffit de recopier le hero existant tel quel dans le fix.

**Trois familles de defaut nouvelles, toutes avec un test relancable :**
1. **La couche `v2-pipeline-enrich/<t>.hero_name_fr.json` degrade le hero.** UNM etait repointe de "Unum US Premium Income" (20 trim) vers "Premium Income" (12 trim), sous le seuil. `canReplaceHero` ne bloque cette couche tardive que si le candidat est GENERIQUE ; un candidat specifique mais moins profond passe. Test : comparer `hero_kpi_override` au `hero_kpi` de la base et les longueurs d history. 57 fiches repointees, 23 ou la base est plus profonde, **mais 22 des 23 sont des faux positifs** (l override est resolu par une autre couche et l audit les donne PASS). Ne pas corriger en masse.
2. **Une fiche dont `v2-pipeline/<t>.json` a `kpis: []` et `hero_kpi: null` rend une page vide**, meme quand les couches `v2-pipeline-specific-kpis` et `v2-pipeline-enrich` sont pleines : elles ne sont PAS fusionnees sans base. C etait le cas de DGE.L et CLNX.MC, online mais hors clean-all. Reconstruites verbatim.
3. **Un `desk_hero_kpi_overrides` peut pointer sur un short qui n existe nulle part.** CLNX.MC visait "Towers Owned EU", present seulement comme entree orpheline de `src/data/_kpis-index.json`. Repointe sur "Tower Sites".

**Tests de qualite relances, resultats :** 21 KPI de revenu en millions etiquetes en milliards corriges sur 9 fiches (unite seule) ; CPRT portait 4 KPI en **milliers** de dollars etiquetes en milliards, doublons d equivalents corrects deja presents, retires ; 29 blocs de repartition sans aucune part chiffree (21 passes en unite pourcentage avec value = share_pct, 8 retires) ; 3 blocs incoherents avec le CA retires apres rejet de re-extraction (MET, COFB.BR, PHG) ; KGF.L portait encore 2 octets nuls dans des champs visibles.

**Ecarter definitivement** (verifies, corrects) : TM et 7203.T dont le CA de 48 036 Mds JPY est juste, LMP.L et LSCC dont les blocs a 0,40 Mds sont exacts, BT-A.L et BMW.DE et V et SNA dont les parts depassent 100 % avant eliminations.

**Pipeline CA** : 141 blocs, ok=23. Les 23 valides un par un avant commit, **4 rejetes a la main** : WPP.L segment (97,3 Mds GBP quand le CA est 14,4, et **deux extractions successives donnent la meme erreur**), NMR segment (ventilation geographique en double de l onglet geographie), BME.L geographie (un semestre face a un segment annuel), MUFG geographie (aucune part chiffree). Tous inscrits dans `done` du checkpoint.

**Arbitrages Yann en attente**, detail dans `.conv-state/v195-session-27aout-reprise.md` : ADTTF (deux KPI de la fiche, `Headcount` et `Memory Tester Revenue`, ont des series trop regulieres pour etre verbatim et sont incompatibles avec le rapport integre 2025, fiche laissee hors ligne) ; ITRK.L (3 KPI specifiques sur 4, il manque un rapport annuel 2026) ; NDA-SE.ST et NDA-DK.CO (pas de fiche `v2-pipeline` du tout) ; BWA (cas structurel du type FLEX, segmentation refondue, aucune serie a 16 trimestres).

**Pieges CLI de ce run** : `for f in $VAR` ne fait pas de decoupage en mots sous zsh, ecrire la liste en clair dans le `for` ; `git show HEAD:<fichier>` sert a lire l indentation d origine, le pipeline CA ecrit en indent 2 et gonfle le diff d un fichier en indent 1 (UBS : 1 176 lignes au lieu de 38) ; un `git push` declenche deja un build, ne jamais appeler le hook en plus, et **grouper les lots** (3 pushes = 3 builds de 9 min empiles).


MAJ 27 aout 2026 ~20h-21h (deuxieme run planifie du jour) : **plus aucune ste publiable, la queue est fermee**. clean-all 871, online 862, les 9 restants sont tous inscrits dans `src/data/_hero-suspect.json` avec leur raison : ALAB, AMRZ.SW, HONA, NBIS, Q, SNDK, SPCX, FDXF sont des scissions ou introductions de 2024-2026 (trop jeunes pour 16 trimestres ou 5 exercices, et **aucune source dans le data-lake**, ni sec-data ni ir-scrape), FLEX est le cas structurel deja connu. **Ne plus re-tenter la boucle de publication tant que Yann n a pas elargi clean-all ou collecte de nouvelles sources.** Le seul candidat borderline etait `SNDK.EDGE_REV` annuel a 5 exercices, ecarte car incoherent avec son propre trimestriel (Edge = 61 % du CA au dernier trimestre mais 92 % sur l exercice) et invérifiable.

Audit qualifieur des 862 en ligne : **857 PASS, 5 FAIL** (APP, RDDT, CRWV, GEV, SW, tous structurels connus). Aucune regression.

**Le travail utile de ce run a ete la qualite, pas la publication.** Trois tests relancables, tous rentables :
1. **Blocs de repartition sans aucune valeur chiffree**, restreint a clean-all : 7 trouves, tous sur les stes publiees le matin meme. Reparation en pourcentage (value = share_pct, unit "%") quand les parts somment a 100, extraction verbatim par agent sinon. SAND.ST et MAP.MC remplis, geographie de MAP.MC retiree faute de source.
2. **Doublons d history sur les fiches en ligne** : 9 paires, 11 KPI retires (HUM, AAPL, ADP, CPT, BG, BBY, AMCR, plus PGR ou des en-tetes de tableau etaient devenus des KPI et SAN.MC ou deux series etaient entierement nulles). **Piege de normalisation** : certaines history sont des listes de {"q","v"} et non de nombres, les lire naivement fabrique des faux positifs (RDDT DAUQ/WAUQ, SCMN.SW REV_Q/OTHER_REV_Q).
3. **Coherence `value` contre le dernier point d `history`** : 22 ecarts sur les 56 fiches rafraichies par le cron, 6 corriges (MUFG trois KPI divises par mille, NVDA et SNPS effectif en dizaines contre une serie en employes, WWD Industrial Revenue a serie plate retire). 16 ecarts moderes laisses en arbitrage, listes dans `.conv-state/v195-session-27aout-reprise2.md`.

**Defaut systemique a traiter** : le cron quarterly-refresh met a jour `value`, `history` et `yoy` mais **laisse `description` intacte**. Sur NVDA la description annoncait encore 120,1 Mds $ annuels pour une valeur trimestrielle de 59 688 M $. Deux descriptions corrigees a la main, le defaut reste ouvert pour les futurs runs.

**Le cron laisse son travail non commite** : 56 fiches `v2-pipeline-enrich` du refresh de 05h55 et la geographie d Informa du pipeline CA de 08h18 trainaient dans l arbre de travail. Toujours verifier `git status` sur `src/data` au demarrage d un run.

**Piege git majeur** : `git add data-lake` avale 12 551 fichiers non suivis et fabrique un commit de 19 000 fichiers et 21 millions de lignes dont le push ne passe pas. Utiliser `git add -u data-lake` pour ne prendre que les fichiers deja suivis.

**Piege lecture** : `json.load` sur `v1-9-5-clean-all-tickers.json` rend un dictionnaire `{generated_at, count, tickers}`, pas une liste. En faire un set directement donne un set de 3 cles et fait passer tous les tests a vide sans erreur.


MAJ 28 aout 2026 00h-01h (run planifie) : **queue de publication toujours fermee**, clean-all 871 et online 862, les 9 restants sont ceux du 27 aout (ALAB, AMRZ.SW, HONA, NBIS, Q, SNDK, SPCX, FDXF, FLEX). Aucune re-tentative faite, aucune a faire.

**Le travail du run a ete un controle qualite systematique, 67 KPI corriges et 20 retires en 4 lots, zero regression** (audit qualifieur des 862 apres coup : 857 PASS / 5 FAIL, exactement le meme resultat que la veille, les 5 FAIL etant APP, RDDT, CRWV, GEV et SW).

**Quatre detecteurs nouveaux, conserves dans `scripts/scan-*.py`** et rejouables a chaque run :
- `scan-scale-mismatch.py` : ecart entre `value` et le dernier point de sa propre `history`, avec classement du facteur (x1000, /1000...). A trouve 18 defauts nets.
- `scan-value-div1000.py` : filtre strict (value au moins 50x sous la serie ET value*1000 dans [0,5 ; 2] fois la serie ET serie homogene par la mediane). 22 defauts, zero faux positif.
- `scan-yoy-mismatch.py` : yoy declare contre yoy calcule sur la serie (lag 4 en quarter, 1 en year).
- `scan-stale-descriptions.py` : description citant un montant dans l unite exacte du KPI, incompatible avec la value.

**Defauts trouves, familles reutilisables :**
1. **value divisee par mille** sous une unite en M $ ou en unites brutes. Le hero de CPRT affichait 3,452 M $ au lieu de 3 451,6 M $. Regle de reparation canonique et sure : `value = dernier point de history`.
2. **echelle inverse** : c est la history qui est cassee, pas la value (MTB, AKZA.AS, TEL2-B.ST, CCI, CHTR). **Ne pas reparer sans source** : remultiplier une serie a 3 decimales donne une serie plate.
3. **unite fausse** : GL Premium et WELL Resident fees etiquetes Mds $ pour une serie en M $. SPG Minimum Rents etait en milliers de dollars.
4. **KPI vides** : serie entierement nulle, ou constante a zero contre une realite non nulle. **HEIA.AS, INGA.AS et VOW.DE avaient un DPS a zero alors qu ils versent tous un dividende**, et chez INGA.AS ce zero **masquait la vraie serie de kpis-haut** (1,086 EUR sur 10 points) : retirer le KPI vide d enrich la laisse remonter.
5. **values textuelles** : le rendu fait `Number(value)`, donc une string numerique propre ne casse rien (356 cas laisses tels quels), mais **5 produisaient un NaN affiche** a cause d une virgule (COST, GEN, LIN x2, WDAY), et 12 etaient vraiment textuelles ("Non disponible" alors que la serie existait chez REGN et CBOE, double pourcent chez ED et PEP, tilde chez ROST et SCI).

**Faux positifs a ne jamais re-signaler** : VICI Charge_Credit_CECL et Rev_Prets dont la description cite la **hausse** et non le niveau ; les AUM au-dela de 1 000 Mds $ sont normaux chez BLK, BX, IVZ, SCHW, UBS, BEN, V et C ; TAP (royalty exclu), WY (10 + 14 = 24 M acres), PWR, SLB, ROST, LUV, WBD.

**Reste ouvert, detaille dans `.conv-state/v195-arbitrages-28aout.md`** : 331 yoy incoherents avec leur serie dont 145 de signe oppose, **volontairement non corriges** car on ne peut pas trancher entre un yoy verbatim juste et une history fausse sans revenir aux sources (exemple AAPL iPad Revenue, yoy +8 % sur une serie annuelle [8,595 ; 6,914] alors que le vrai CA iPad est autour de 26 Mds $). Plus 3 effectifs a point aberrant (FAST, NOW, GE), 20 dates au 31/12/2026 et 108 KPI de plus de 900 jours.

**Piege confirme ce run** : `build-v2-pipeline-merged.ts` ecrit `_merged.json` **en compact sur une seule ligne** (42 Mo) alors que la version commitee etait en pretty-print (52 Mo), d ou un diff de 1,5 million de lignes qui ressemble a une perte de donnees. Verifier par comparaison semantique des cles avant de paniquer : ici 2307 cles des deux cotes, une seule entree differente (VMRK, accents FR corriges en amont). `_merged.json` ne contient **pas** les donnees de `v2-pipeline-enrich`, donc modifier enrich ne demande aucun rebuild.
